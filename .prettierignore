pnpm-lock.yaml
uv.lock
dist/
build/
node_modules/
packages/contracts/generated/
packages/api-client/src/generated/
services/worker/src/job_getter_worker/contracts/generated/

# The specification package is the immutable input to this build. Reformatting
# it would create diff noise against the document we are implementing from.
docs/spec/

# Generated artifacts and vendored fixtures.
#
# The fixture corpus is produced by fixtures/generate.py and pinned
# byte-for-byte by MANIFEST.sha256. Prettier collapses short JSON arrays onto
# one line where json.dumps expands them, so formatting these files made the
# committed bytes disagree with the manifest and failed CI on a fresh clone
# while passing locally. Generated output is never formatted by hand.
fixtures/MANIFEST.sha256
fixtures/cvs/
fixtures/model-responses/
fixtures/jobs/
fixtures/ats-pages/
# Chromium's reading of the confirmation fixtures, written by
# fixtures/fill-planner/generate-confirmation.py and asserted against as written.
fixtures/fill-planner/greenhouse-confirmation.json
apps/api/dist/
apps/web/dist/

# Generated by src/db/bundle-migrations.ts and compared byte-for-byte against
# the generator by tests/db/packaging.test.ts. Formatting it would make the
# drift check fail for a reason that has nothing to do with the migrations.
apps/api/src/db/migrations/bundled.ts
